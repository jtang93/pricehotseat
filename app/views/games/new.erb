<h1>Start New Game</h1>

<%= form_for @game do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :num_rounds %>
  <%= f.number_field :num_rounds %>

  <%= f.submit %>
<% end %>
